import makeStyles from '@material-ui/core/styles/makeStyles'

export const useStyles = makeStyles({
    root: {
        display: 'flex',
    },
    goRight: {
        marginLeft: 'auto',
    },
    button: {
        whiteSpace: 'nowrap',
        padding: '16px 24px',
    },
})
